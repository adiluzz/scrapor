# Core worker infra
psycopg[binary]==3.2.3
redis==5.2.1
boto3==1.35.71
yt-dlp>=2026.6.9   # keep current — yt-dlp must track site changes; bump the floor to refetch on rebuild

# Adult-site search APIs (EchterAlsFake ecosystem). These are async-first and
# break their public API frequently, so pin exact versions — scripts/site_searchers.py
# is written against these. Any that fail to import/search are skipped gracefully
# per-source at runtime.
phub==5.1.2
xnxx-api==2.1
xvideos-api==2.2.2
youporn-api==1.5.2
hqporner-api==2.1.1
# Shared base of all the above. They pull it in unpinned, and 4.x removed
# `setup_logger`, which makes every one of them fail to import — pin the last
# compatible release so a rebuild can't silently disable the API searchers.
eaf-base-api==3.3.2
# HTML scraping for sources without a working API (Eporner, RedTube, SpankBang).
curl_cffi>=0.7.0
beautifulsoup4>=4.12.0

# Scene detection for v2 hover previews (short montage from shot boundaries)
scenedetect[opencv]>=0.7
